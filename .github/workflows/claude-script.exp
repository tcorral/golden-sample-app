#!/usr/bin/expect -f
set timeout -1
spawn npx claude config add allowedTools Edit && npx claude config add allowedTools Replace && npx claude -p "Generate a Dockerfile for a Node.js 22 app"
expect "^"
# send "\x04"  # Ctrl+D to exit
expect eof