#!/usr/bin/expect -f
set timeout -1
# Get the command-line argument (the prompt)
set prompt [lindex $argv 0]
spawn npx claude config add allowedTools Edit && npx claude config add allowedTools Replace && npx claude -p "$prompt"
expect "^"
# send "\x04"  # Ctrl+D to exit
expect eof